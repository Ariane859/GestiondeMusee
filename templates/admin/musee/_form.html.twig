{{ form_start(form) }}
        <div class="row">
            <div class="col-md-2">{{ form_row(form.numMus) }}</div>
            <div class="col-md-2">{{ form_row(form.nomMus) }}</div>
            <div class="col-md-2">{{ form_row(form.nblivres) }}</div>
            <div class="col-md-4">{{ form_row(form.codePays) }}</div>
        </div>
            {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button|default('Ajouter') }}</button>
{{ form_end(form) }}