{% extends 'base.html.twig' %}

{% block title %}Pays{% endblock %}

{% block body %}
   <div class="container mt-4">
    <h1>Pays</h1>
    
     <table class="table">
        <tbody>
        <tr>
            <th>Code du Pays :</th>
             <td>{{ pays.codePays }}</td>
        </tr>   
        <tr>
            <th>Nbre d'habitant : </th>
            <td>{{ pays.nbhabitant }}</td>
        </tr>
        </tbody>
    </table>
    <div class="text-right">
    <a href="{{ path('admin.pays.index') }}" class="btn btn-primary">Retourner à la liste</a>
    </div>  
    <a href="{{ path('admin.pays.edit', {'id': pays.id}) }}" class="btn btn-primary">Editer</a>
    
    {{ include('admin/pays/_delete_form.html.twig') }}
    
{% endblock %}
