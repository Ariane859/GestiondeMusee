{{ form_start(form) }}
        <div class="row">
            <div class="col-md-4">{{ form_row(form.codePays) }}</div>
            <div class="col-md-4">{{ form_row(form.nbhabitant) }}</div>
        </div>
            {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button|default('Ajouter') }}</button>
{{ form_end(form) }}