{{ form_start(form) }}
        <div class="row">
            <div class="col-4">{{ form_row(form.numMus) }}</div>
            <div class="col-4">{{ form_row(form.ISBN) }}</div>
            <div class="col-4">{{ form_row(form.dateAchat) }}</div>
        </div>
            {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button|default('Ajouter') }}</button>
{{ form_end(form) }}