{% extends 'base.html.twig' %}

{% block title %}Ouvrage{% endblock %}

{% block body %}
   <div class="container mt-4">
    <h1>Ouvrage</h1>
    
     <table class="table">
        <tbody>
            <tr>
            <th>ISBN :</th>
            <td>{{ ouvrage.ISBN }}</td>
            </tr>
            <tr>
            <th>Nbre de Page :</th>
            <td>{{ ouvrage.nbPage }}</td>
            </tr>
            <tr>
            <th>Titre :</th>
            <td>{{ ouvrage.titre }}</td>
            </tr>
            <tr>
            <th>Code du pays :</th>
            <td>{{ ouvrage.codePays }}</td>
            </tr>
        </tbody>
    </table>
    <div class="text-right">
    <a href="{{ path('admin.ouvrage.index') }}" class="btn btn-primary">Retourner à la liste</a>
    </div>  
    <a href="{{ path('admin.ouvrage.edit', {'id': ouvrage.id}) }}" class="btn btn-primary">Editer</a>
    
    {{ include('admin/ouvrage/_delete_form.html.twig') }}
    
{% endblock %}
