{{ form_start(form) }}
        <div class="row">
            <div class="col-md-4">{{ form_row(form.ISBN) }}</div>
            <div class="col-md-4">{{ form_row(form.nbPage) }}</div>
            <div class="col-md-4">{{ form_row(form.titre) }}</div>
            <div class="col-md-4">{{ form_row(form.codePays) }}</div>
        </div>
            {{ form_widget(form) }}
        <button class="btn btn-primary">{{ button|default('Ajouter') }}</button>
{{ form_end(form) }}