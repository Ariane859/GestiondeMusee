{% extends 'base.html.twig' %}

{% block title 'Editer' %}

{% block body %}

    <div class="container mt-4">
        <h1>Editer un site</h1>
        {{ include('admin/site/_form.html.twig', {button: 'Editer'}) }}
        <br><a href="{{ path('admin.site.index') }}" class="btn btn-primary">Retourner à la liste</a>
    </div>
{% endblock %}




