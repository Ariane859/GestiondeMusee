{% extends 'base.html.twig' %}

{% block title %}Moment{% endblock %}

{% block body %}
   <div class="container mt-4">
    <h1>Moment</h1>
    
     <table class="table">
        <tbody>
            <tr>
                <th>Jour :</th>
                <td>{{ moment.jour ? moment.jour|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
    <div class="text-right">
    <a href="{{ path('admin.moment.index') }}" class="btn btn-primary">Retourner à la liste</a>
    </div>  
    <a href="{{ path('admin.moment.edit', {'id': moment.id}) }}" class="btn btn-primary">Editer</a>
    
    {{ include('admin/moment/_delete_form.html.twig') }}
    
{% endblock %}
